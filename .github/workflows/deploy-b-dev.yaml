name: deploy b/dev
on:
  push:
    branches:
      - main
    paths:
      - files/b.txt
defaults:
  run:
    shell: bash
jobs:
  dev:
    runs-on: ubuntu-latest
    environment: b/dev
    steps:
      - uses: actions/checkout@v6
      - run: cat files/b.txt
